<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Read these first

- **@PROJECT.md** — what this site is, architecture (routes.ts is the single source of truth, content-as-data engine), locales (LT is default at `/`, EN at `/en`), how to publish an article, what's shipped, what's next, open items.
- **@BRAND.md** — the AV Brand System (five colors, two fonts, `.avb` cards). Never use ad-hoc colors.

Hard rules: content must be deep and researched (not thin listicles), native in each language (never translated), shipped in **both** LT + EN, no em-dashes, and **always get hero-image approval before wiring one in**.
